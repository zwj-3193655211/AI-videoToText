# ===== 核心运行依赖 =====
# ASR 引擎（funasr 会自动带入 scipy/librosa/modelscope 等子依赖）
funasr>=1.2.6
torch>=2.0.0
torchaudio>=2.0.0
modelscope>=1.9.5

# GUI
PySide6>=6.4.0

# 数据处理 / 音频捕获
numpy>=1.24.0
soundcard>=0.4.2

# LLM 后端（DeepSeek API / Ollama）
requests>=2.28.0
python-dotenv>=1.0.0

# 文本处理
zhconv>=1.4.0

# ===== 可选依赖 =====
# faster-whisper：ASR 可选后端（非默认，需自行下载 whisper 模型）
#   如需使用：pip install faster-whisper>=1.0.0
#
# ffmpeg：视频转音频需要系统安装 ffmpeg 命令（https://ffmpeg.org/）
#   Windows: winget install ffmpeg 或下载安装包后加入 PATH
